import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
  googleButton: {
    display: 'flex',
    flexDirection: 'row',
    alignContent: 'center',
    paddingLeft: '0',
    paddingRight: '8px',
    border: 'thin solid #888',
    borderRadius: '6px',
    gap: '8px',
    height: '40px',
  },
});
