import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
  badgeContainer: {
    width: '24px',
    height: '24px',
    padding: 0,
    borderRadius: '50%',
  },
  badgeInner: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '24px',
    height: '24px',
  },
});
