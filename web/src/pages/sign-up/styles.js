import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
  githubButton: {
    display: 'flex',
    flexDirection: 'row',
    alignContent: 'center',
    paddingLeft: '8px',
    paddingRight: '8px',
    border: 'thin solid #888',
    borderRadius: '6px',
    gap: '21px',
    height: '40px',
  },
});
